{% extends 'base.html.twig' %}

{% block title %}Editer un tag{% endblock %}

{% block body %}

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.isPublish) }}
    <button type="submit">Editer un tag</button>
    {{ form_end(form) }}

{% endblock %}
